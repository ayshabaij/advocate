<%= form_with(model: junior, local: true) do |form| %>
  <% if junior.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(junior.errors.count, "error") %> prohibited this junior from being saved:</h2>

      <ul>
        <% junior.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password %>
  </div>

  <div class="field">
    <%= form.label :password_confirmation %>
    <%= form.password_field :password_confirmation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
